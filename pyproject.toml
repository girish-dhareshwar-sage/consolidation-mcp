[project]
name = "consolidation-mcp"
version = "0.1.0"
description = "Consolidation for MCP: A tool to consolidate and manage your MCP projects efficiently."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "mcp[cli]>=1.26.0",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
]
